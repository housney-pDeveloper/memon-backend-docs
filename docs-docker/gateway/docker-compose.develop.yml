name: platform-ef-develop

services:
  platform-ef-develop-gateway:
    image: gitlab.fingate.kr/platform/ef/backend/gateway:develop
    container_name: platform-ef-develop-gateway
    restart: unless-stopped

    working_dir: /home/ec2-user

    environment:
      SPRING_PROFILES_ACTIVE: develop
      DOCKER_FOREGROUND: "true"
      APP_TZ: Asia/Seoul

      AWS_PROFILE: epk-dev
      AWS_REGION: ap-northeast-2
      AWS_SHARED_CREDENTIALS_FILE: /aws-creds/credentials
      AWS_CONFIG_FILE: /aws-creds/config

    volumes:
      - /root/.aws:/aws-creds:ro
      - /etc/localtime:/etc/localtime:ro
      - /home/fingate/docker/volumes/platform/ef-develop:/home/ec2-user

    entrypoint:
      - /bin/bash
      - -lc
      - |
        chmod +x /home/ec2-user/deploy/gateway/scripts/*.sh 2>/dev/null || true
        exec /home/ec2-user/deploy/gateway/scripts/deploy.sh

    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:9999/actuator"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

    networks:
      - edge
      - platform-ef-develop-network

    labels:
      - traefik.enable=true
      - traefik.docker.network=edge
      - traefik.http.routers.platform-ef-develop-gateway.rule=Host(`eventflow-gw-dev.fingate.kr`)
      - traefik.http.routers.platform-ef-develop-gateway.entrypoints=web
      - traefik.http.services.platform-ef-develop-gateway.loadbalancer.server.port=9999

networks:
  edge:
    external: true
  platform-ef-develop-network:
    external: true
